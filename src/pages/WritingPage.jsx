import { SEO } from '../app/seo'; import { WritingCard } from '../components/cards/WritingCard'; import { PageIntro } from '../components/ui/PageIntro'; import { WRITINGS } from '../data/siteContent'; import { useDocumentMeta } from '../hooks/useDocumentMeta'; import { useReveal } from '../hooks/useReveal';
export function WritingPage(){useDocumentMeta(...SEO.writing);useReveal();return <main className="page-main"><PageIntro index="03" title="学习内容" description="记录我对 AI 训练、数据质量、标注方法和模型评测的理解与复盘。"/><section className="section writing-page-list">{WRITINGS.map(item=><WritingCard key={item.title} article={item}/>)}</section></main>}
